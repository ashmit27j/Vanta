"""Entry point for the `purplelab` CLI.

STUB — the real command surface (pick, run, check, log, today, sigma
convert/deploy, coverage, containment-check, detonate) is built out across
Prompts 5-8 in docs/prompt-chain.md.
"""

import sys


def main() -> int:
    print("purplelab is not implemented yet (see docs/prompt-chain.md, Prompt 5).")
    return 1


if __name__ == "__main__":
    sys.exit(main())
