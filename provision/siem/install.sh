#!/usr/bin/env bash
# Provisions siem-vm: Docker + Wazuh + INetSim.
#
# STUB — filled in by Prompt 2 (Wazuh) and Prompt 3 (INetSim + containment).
# Must be idempotent when implemented: safe to re-run on a partially
# provisioned or already-provisioned box.
set -euo pipefail

echo "provision/siem/install.sh is not implemented yet (see docs/prompt-chain.md, Prompt 2)."
exit 1
