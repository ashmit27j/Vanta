#!/usr/bin/env bash
# Provisions victim-vm: Wazuh agent enrollment, auditd, Sysmon-for-Linux,
# PowerShell + Invoke-AtomicRedTeam, and a locked-down samples working dir.
#
# STUB — filled in by Prompt 4. Must be idempotent when implemented: safe to
# re-run on a partially provisioned or already-provisioned box.
set -euo pipefail

echo "provision/victim/install.sh is not implemented yet (see docs/prompt-chain.md, Prompt 4)."
exit 1
